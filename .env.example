# ── KRX 로그인 (pykrx 1.2.x 필수) ───────────────────────────
# KRX 데이터 포털(data.krx.co.kr) 계정
KRX_ID=
KRX_PW=

# ── 텔레그램 봇 ─────────────────────────────────────────────
# @BotFather 로 봇 생성 후 토큰, 메시지 받을 chat_id
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── 뉴스 헤드라인 (선택) ────────────────────────────────────
# 네이버 개발자센터 검색 API 키. 없으면 📰 줄은 생략된다.
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# ── 필터 임계값 덮어쓰기 (선택) ─────────────────────────────
# SCREEN_VALUE_EOK=2000         # 거래대금 하한(억)
# SCREEN_CHANGE_PCT=10          # 등락률 하한(%)
# SCREEN_MARKETCAP_JO=30        # 시총 상한(조)
# SCREEN_SURGE_RETURN_PCT=100   # 단기 급등 지속 기준(3개월 %)
